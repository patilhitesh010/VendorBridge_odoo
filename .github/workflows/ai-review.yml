name: AI Code Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: AI Review
        uses: actions/github-script@v7
        with:
          script: |
            // This is a placeholder for the AI review script.
            // It requires an ANTHROPIC_API_KEY secret to function fully.
            console.log("PR Review Triggered");
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
